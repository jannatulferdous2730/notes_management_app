import 'package:flutter/material.dart';

/// A fixed palette of soft, Material-3-friendly note accent colors.
/// Always use one of these — never a free-form color value.
const List<Color> noteColorPalette = [
  Color(0xFFE8F5E9), // Soft Mint Green
  Color(0xFFFFF9C4), // Soft Lemon Yellow
  Color(0xFFE3F2FD), // Soft Sky Blue
  Color(0xFFFCE4EC), // Soft Rose Pink
  Color(0xFFF3E5F5), // Soft Lavender
  Color(0xFFFFF3E0), // Soft Peach
  Color(0xFFE0F7FA), // Soft Aqua
  Color(0xFFF9FBE7), // Soft Lime
];

/// Convenience: the default color for new notes.
const Color defaultNoteColor = Color(0xFFE8F5E9);

/// Default color value (int) for new notes.
const int defaultNoteColorValue = 0xFFE8F5E9;
